# Shepherd

Read and follow `AGENTS.md`. Your role comes from the dispatched ticket and
responsibility map, not the name of this instruction file. A review worker must
be independent of implementation. Link the coordinator-observed worker binding;
report only self-visible telemetry, using Unavailable for unseen fields. Never copy
a supervisor ID into the independent worker field.
